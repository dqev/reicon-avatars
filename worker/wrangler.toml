name = "reicon-avatars-api"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Custom Domain Configuration
routes = [
  { pattern = "avatars.reicon.dev", custom_domain = true }
]

[vars]
ENVIRONMENT = "production"
MAX_REQUESTS_PER_MINUTE = 100
